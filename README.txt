TradeStreamPro frontend ready for Vercel.

Upload ALL files inside this folder to GitHub, not the folder itself.

Before testing image upload:
1. Open src/App.jsx
2. Replace REPLACE_WITH_RENDER_BACKEND_URL with your Render backend URL.
Example:
const BACKEND_URL = "https://tradestreampro-backend.onrender.com/read-image";
